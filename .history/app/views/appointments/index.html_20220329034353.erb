<h1>Appointments#index</h1>
<p>Find me in app/views/appointments/index.html.erb</p>

<%= form_with url: "/appointments/show", method: :get do |form| %>
<div class="row">
    <div class="col">
        <%= form.label :date, "Select date", class: "form-label" %>
        <%= form.date_field :date, class: "form-control" %>
    </div>

    <div class="col">
        <div class="form-check">
            <%= form.label :duration, "30 minutes", class: "form-check-input" %>
            <%= form.radio_button :duration, "30", class: "form-check-label" %>
        </div>
        <div class="form-check">
            <%= form.label :duration, "60 minutes", class: "form-check-input" %>
            <%= form.radio_button :duration, "60", class: "form-check-label" %>
        </div>
    </div>
</div>
  <%= form.collection_check_boxes :ids, @therapists.order(:name), :id, :name %>

  <%= form.submit "Submit" %>

<% end %>
